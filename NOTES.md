# NOTES — integration log (becomes the World debrief and 1inch/ENS feedback)

Log as you go: timestamp · sponsor · what you tried · what broke · how long it cost · what doc was missing.

## World
- 2026-09-26 12:05 JST · FIRST REAL PROOF · real backend on real Sepolia, real IDKit in a browser (Playwright, no stub), connector URI handed to World's Simulator MCP (`simulator.worldcoin.org/api/mcp`, `complete_test_request`). Portal accepted the proof; backend sent grantRoles(ORB) + verify(); page moved to A′ "Verified · Orb, up to 15,000 USDC". On-chain: agent roles = MANDATE|ORB, lastVerified stamped. Credential identifier confirmed: `proof_of_human` → orb tier. Nonce is a 66-char 0x hex string; signature 132 chars.
- 2026-09-26 12:00 JST · simulator constraint · first attempt failed `proof_of_human_required` at stage bridge_request: the simulator completes only a single Proof-of-Human request, not `any(proof_of_human, passport, mnc, selfie)`. Added `WORLD_CREDENTIALS` (backend → rp-context → page builds the constraint). Staging: `proof_of_human`. Production phone: all four, so World App offers whatever Alice holds. Cost ~15 min.
- 2026-09-26 11:30 JST · portal via MCP · The Developer Portal MCP (developer.world.org/api/mcp, team API key) did the whole setup from the CLI: existing app `app_29bb7ef1643470c4e5535a8468422e24` → `configure_world_id` (managed RP `rp_069e54311421c6ec`, KMS manager key, signing key returned once, on-chain registration pending → registered on production and staging within a minute) → `create_world_id_action leash-verify` → `set_world_id_staging_verification` (24 h window + token). Friction: MCP-created apps are production-only; staging proofs need that window AND an `x-staging-verification-token` header on every /verify call, which no IDKit README mentions. Python's default User-Agent gets 403; the endpoint returns relative paths on developer.world.org (not developer.worldcoin.org), so both hosts are live.
- 2026-09-26 11:35 JST · live probe · real rp_context signed by our backend + a deliberately fake proof → portal answered `All proof verifications failed` (HTTP 400): RP id, host, staging header and untouched forwarding all accepted; no transaction sent. Portal docs confirm "v4 accepts nullifier reuse", which matches our nonce-based replay design.
- 2026-09-26 07:30 JST · one request, every tier · IDKit 4.3.0's browser build (`IDKitBundle`, sets `window.IDKit`) exposes `any()` and `CredentialRequest(type)`. `request(...).constraints(any(proof_of_human, passport, mnc, selfie))` lets World App offer whichever credential the user has, and the backend maps the returned identifier to a tier. Presets like `selfieCheck()` would force a single tier. The build loads `idkit_wasm_bg.wasm` relative to its own script URL, so both are served from `/vendor/`.
- 2026-09-26 06:30 JST · verify host · IDKit 4.3.0's own README posts to `https://developer.worldcoin.org/api/v4/verify/{rp_id}`, not `developer.world.org` as our handoff said. Using the package's host (overridable via WORLD_VERIFY_URL).
- 2026-09-26 06:30 JST · replay model · a v4 nullifier is fixed per (human, action), so "reject a reused nullifier" would block the same human re-verifying, which is Leash's whole renewal loop. Replay key is instead the rp_context `nonce` (IDKitResultV4.nonce), which our backend signs, stores and consumes once; the nullifier is bound to one Vault so one human cannot keep two agents alive. Neither the docs nor the handoff mention this tension.
- 2026-09-26 06:30 JST · credential ids · from IDKit types: `proof_of_human` (issuer 1), `passport` (9303), `mnc` (9310), `selfie` (11). Mapped to orb / document / document / selfie. Still unconfirmed against a live staging proof.

## ENS
- 2026-09-26 04:00 JST · delegation · Plan was: Alice grants the backend tier-admin bits on the `agent` token. Impossible: `PermissionedRegistry._getSettableRoles` returns `roleBitmap >> 128` for token resources, so token admin bits exist only if passed at `register()` and can never be delegated. Root roles are exempt and `hasRoles` = root | token, so the backend gets `(ORB|DOCUMENT|SELFIE) << 128` at ROOT via `grantRootRoles`. It can set tiers on any name in Alice's registry but can never grant or revoke MANDATE. Proven on fork (test/fork/ENSAuthority.t.sol). Cost ~30 min reading source; not in any doc we found.
- 2026-09-26 04:00 JST · reads · `roles(anyId, account)` returns token roles only (root excluded); `hasRoles` includes root. `uint256(keccak256(label))` (version bits zero) is a valid anyId for both, so a contract can skip `findTokenId`. Every grant/revoke regenerates the token id (burn + mint, `tokenVersionId++`).
- 2026-09-26 04:00 JST · gas (cold, fork) · roles() 29.6k · hasRoles() 32.1k · findTokenId() 16.0k.
- 2026-09-26 04:00 JST · test trap · `makeAddr("alice")` on a Sepolia fork has an EIP-7702 delegation (`0xef0100...`), so the ERC1155 mint's `onERC1155Received` reverts. Use unique labels (`leash.alice`).

## 1inch
- 2026-09-26 13:40 JST · cap-token hole closed · the gate only trimmed when one leg was USDC, so a HYPE/WETH pool would have been ungated. Now `Vault.ship` rejects token lists without the cap token (`CapTokenMissing`) and the gate reverts `MandateTokenMissing` on any such strategy, even one shipped around the Vault. Router 22,509 B. Both checks mutation-tested.
- 2026-09-26 13:50 JST · strategies · one program for every pair (`MandateGate → XYCSwap → Salt`); `Agent.s.sol` takes `OTHER`, `POOL_USDC`, `POOL_OTHER`. Proven on a fork with an 8-decimal WBTC demo token: 0.1 WBTC / 10,000 USDC pool, 20,000 ask trimmed to 14,500, feed reads the token's `symbol()` ("Market trade on WBTC/USDC"). Other curves (XYCConcentrateSwap, PeggedSwap) would need their own inverse in the gate; not built.
- 2026-09-26 08:30 JST · token order · `MakerTraitsLib.build` requires tokenA < tokenB by address. Our gate tests passed only because USDC happened to deploy at the lower address; on the real Sepolia deploy HYPE sorts first and a USDC-first order reverts. LeashOrder sorts; gate tests now run in both orders.
- 2026-09-26 08:30 JST · deploy · creating MandateAquaRouter inside a forge script fails with "Failed to decode constructor arguments ... type check failed for offset" (forge bug with this via-IR contract). `forge create` works, so script/deploy.sh deploys the router separately. Cost ~20 min.
- 2026-09-26 08:30 JST · EOA takers · takerTraits `useTransferFromAndAquaPush` + `isFirstTransferFromTaker` lets a plain wallet trade against an Aqua strategy (router pulls and pushes to Aqua), no callback contract needed. The feed recovers the taker's ask by decoding `swap(order, amount, ...)` calldata, so it can show "Asked 10,000 · allowed 1,967".
- 2026-09-26 06:00 JST · program order · HANDOFF/CLAUDE said "FeeProtocol must be the first instruction". Not true on swap-vm main @ feb1641: FeeProtocol is optional, docs/PROGRAMS.md only says fee placement is security-critical. Leash program: MandateGate -> XYCSwap -> Salt (no fee). Removed the rule.
- 2026-09-26 06:00 JST · computed-leg trimming · the gate runs before XYCSwap, so when the cap token is the leg the curve computes (exact-in selling HYPE for USDC, or exact-out buying HYPE with USDC) it trims the taker's leg with the inverse of x*y=k. Couples the gate to XYCSwap; documented in MandateGate.sol. Fuzzed: the USDC leg never exceeds capNow().
- 2026-09-26 06:00 JST · Aqua strategy = abi.encode(order); Aqua's keccak256(strategy) equals SwapVM `hash(order)` only in that encoding. Asserted in GateBase._ship.
- 2026-09-26 06:05 JST · MandateAquaRouter 22,493 B (margin 2,083). Gate overhead: 26.6k gas per swap cold on real ENSv2 (119.5k vs 92.9k), 4.6k warm with a mock registry.
- 2026-09-26 02:40 JST · setup · swap-vm `main` @ feb1641 pulls Aqua as an npm dep (`@1inch/aqua` github#v1.0.0), not a submodule. Adding 1inch/aqua separately would give two copies. Fix: `cd lib/swap-vm && yarn install --frozen-lockfile --production --ignore-scripts`, remap to `lib/swap-vm/node_modules/*`. Cost ~5 min.
- 2026-09-26 02:45 JST · size · AquaSwapVMRouter is 21,981 B on main @ feb1641 (v0 measured 20,858 B on an older main). Margin 2,595 B; MandateGate (+455 B in v0) still fits, ~2.1 KB left. Re-check on every pull.
- 2026-09-26 02:45 JST · Aqua semantics · `pull()` does `safeTransferFrom(maker, ...)` from Aqua, so a contract maker must `approve(aqua)`. `strategyHash = keccak256(strategy)`, and a docked hash can never be re-shipped (StrategiesMustBeImmutable), so every re-ship needs a fresh `Salt`. `dock()` needs the full token list, not just the hash.

## Decisions
- 2026-09-26 12:30 JST · Vault owner key · The Envoyage deployer key we reused as Alice is an EIP-7702-delegated EOA AND still in use by another process (nonce 2847 → 2932 in ~10 h). Delegated accounts may hold only ONE in-flight tx, so the first rehearsal's `setCap` died with "in-flight transaction limit reached for delegated accounts" (viem shows only "Missing or invalid parameters" unless you read `details`). Fix: fresh plain key `0x7EbC…34Ef` is the Vault owner (Vault v3 `0x4795…C2B9`); the deployer keeps leash.eth and the registry, and granted the new owner root MANDATE/tier admin so it can revoke and restore. API errors now append viem's `details`.
- 2026-09-26 · deps · swap-vm as git submodule (branch main); Aqua + OZ + forge-std via swap-vm's own node_modules. Root foundry.toml mirrors swap-vm (0.8.30, via_ir, 700 runs) with `lib/swap-vm/` prefixed remappings; `test = "test"` so swap-vm's slow suite never compiles.
- 2026-09-26 · Vault decay · kept halve-per-24h + linear interpolation, added hard zero at elapsed >= 72h (plain halving takes ~31 days to reach 0 on 2000e6). Side effect: a cliff from ~base/8 to 0 at 72h (selfie: ~250 USDC -> 0). UI should show it as "leash runs out", not a smooth slope.
- 2026-09-26 · Vault demo clock · immutable `speed` multiplies elapsed; 1 in tests, 14_400 on the demo Vault (1s = 4h, State C after 18s).
- 2026-09-26 · Vault baseCap · USDC 6-dec units; highest tier bit the agent holds (orb 15k > document 7.5k > selfie 2k), then min with owner-set `ownerCap`. `ownerCap == 0` means no mandate yet (State A). Cap stays 0 until the first `verify()`.
- 2026-09-26 · Vault dock · reads no ENS state, stores app + token list per strategy hash (Aqua.dock needs both). App passed per ship, so MandateAquaRouter can replace the stock router without a Vault redeploy.
- 2026-09-26 · Vault gas (MockEAC, real Aqua) · ship 256,602 (first ship: 2 cold approvals + position storage) · dock 48,329 · verify 44,933 · capNow 20,374. Real ENSv2 hasRoles ~24.4k each and baseCap does up to 3 + findTokenId, so expect ship +~75-100k on Sepolia. Re-measure on the fork in phase 2.
- RESOLVED (speed = 1_440) · demo speed vs 12s blocks · contract only sees block.timestamp; at speed 14_400 one Sepolia block = 48h of decay, so the cap goes base -> base/4 -> 0 in two blocks. Demo steps 2-5 have no runway. Needs a speed choice (720-1440 gives 3-6 min to zero) before the demo Vault deploy.
- PARTLY RESOLVED (Vault.capToken added; gate logic in phase 4) · phase 4 units · capNow() is USDC 6-dec, but MandateGate trims amountIn/amountOut in the swap token's units (CATATAN #6). Gate must clamp the USDC leg only; Vault may need an immutable capToken. Decide before the router.
- 2026-09-26 · ENS · IEAC `findTokenId(string)` matches v0's compile against contracts-v2 PermissionedRegistry. Still re-check against the 15 Sep Sepolia deployment in phase 2.
- 2026-09-26 04:20 JST · Vault gas on real ENSv2 (fork) · switched to one `roles(labelId, agent)` read: ship 256,420 (same as mock) · capNow 33,422 cold. capNow is the per-swap cost MandateGate will add in phase 4 (v0 measured 25.7k with a bare hasRoles).
- 2026-09-26 04:40 JST · infra · publicnode Sepolia is not an archive node: an Anvil fork started at head fails within minutes with "historical state ... is not available". Tenderly's public gateway (`https://sepolia.gateway.tenderly.co`) served code and storage 2,000 blocks back; using it as SEPOLIA_RPC. Still one operator; get a QuickNode key for the demo deploy.
- 2026-09-26 04:45 JST · deploy dry run · `script/DeployLeash.s.sol` on an Anvil fork (Tenderly): registry, agent name, MANDATE grant, backend tier-admin, Aqua, Vault all succeed. Drove the flow: capNow 0 → setCap + backend SELFIE + verify → 2,000 USDC → 4 blocks later 1,183 USDC; backend grant of MANDATE reverts.
- 2026-09-26 · deploy trap · Alice's address must have no code: ERC1155 mint calls `onERC1155Received` on contracts and 7702-delegated EOAs. Use a fresh key, not an Anvil default or a well-known test key.
- 2026-09-26 05:10 JST · Sepolia deploy · DeployLeash + RegisterName broadcast from the Envoyage deployer (EIP-7702-delegated EOA; its delegate accepts ERC1155, confirmed by fork simulation first). leash.eth registered to Alice, subregistry = her UserRegistry; agent holds MANDATE; backend holds root tier-admin (read back on-chain). Cost ~0.0040 ETH total; deployer has ~0.0058 left. Addresses in deployments/sepolia.json.
- 2026-09-26 07:30 JST · e2e · Playwright on an Anvil fork caught a real bug: owner actions (revoke, withdraw) went out as GET because `api(path, undefined)` means GET, so they 404'd silently and the next poll hid the error. Also two driver bugs of my own: a stale `window.__world` handle from a cancelled attempt answered the wrong request, and exact "2,000" assertions flaked because Anvil time follows wall time (1 real second = 0.4 demo hours). Assertions are now ranges; the handle is cleared before every click.
- 2026-09-26 10:40 JST · Sepolia v2 · script/deploy.sh with USER_REGISTRY/AQUA/USDC/HYPE reused: Vault v2 0x52e6e4C8944A84D9637360dAD679bc9B7448B164, MandateAquaRouter 0xa091409BA5C9c6Cae2Db2e924f6b460b44Dfb62f (router.AQUA() checked). Cost ~0.0068 ETH. leash.eth still points at the same registry, so no ENS change was needed.
- 2026-09-26 12:25 JST · FULL REHEARSAL ON REAL SEPOLIA (Vault v3, no stubs) · 11:20:48 proof via simulator → 11:21:12 Create mandate (15,000) → 11:21:24 agent ships → 11:21:48 market asks 10,000, mined fill 7,500 (cap at exactly 24 demo-hours) → 11:23:50 cap 0, State C → trade reverts MandateEmpty → 11:24:00 agent docks → 11:24:36 second proof → B at 14,881. Every figure read back from the page, which reads the chain.
- 2026-09-26 12:25 JST · trap · `forge script` console output comes from the pre-broadcast simulation. The trade printed "filled 10,000" while the mined block trimmed to 7,500, because ~20 s of decay passed between simulation and inclusion. Labels now say "simulated"; the feed decodes the real Swapped event.

